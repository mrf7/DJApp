name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Build
        run: ./gradlew assembleRelease
      - name: Run Tests
        run: ./gradlew test
      - name: Run Detekt
        run: ./gradlew detekt
