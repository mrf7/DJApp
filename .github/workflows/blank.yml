name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Kotlin Linter 
        uses: vroy/gha-kotlin-linter@v2
      - name: setup-android
        uses: msfjarvis/setup-android@0.2
